---
name: example-command
description: Example command — replace with what the command does
argument-hint: "<what the command expects as arguments>"
---

# /example-command — Example Command

Basic description here.

## Invocation

```
/example-command do something
/example-command [some-argument]
```

## Workflow

### Step 1: First step

Do something in this step. The user's arguments are available as `$ARGUMENTS`.

### Step 2: Second step

Do something else here.

## Notes

Notes here.
